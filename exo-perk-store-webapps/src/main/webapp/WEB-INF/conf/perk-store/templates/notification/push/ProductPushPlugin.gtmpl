<%
  String message = null;
  if("true".equals(isNewProduct)) {
   message = _ctx.appRes("Notification.perkstore.product.createdWithTitle", senderName, productTitle);
  } else {
   message = _ctx.appRes("Notification.perkstore.product.modifiedWithTitle", senderName, productTitle);
  }
%>
<%= message %>
